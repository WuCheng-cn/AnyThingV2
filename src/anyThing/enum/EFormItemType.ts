/**
 * # 表单类型
 */
export enum EFormItemType {
  INPUT = 'input',
  INPUT_NUMBER = 'inputNumber',
  SELECT = 'select',
  DATE = 'date',
  DATETIME = 'datetime',
  DATE_RANGE = 'dateRange',
  DATETIME_RANGE = 'datetimeRange',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  TEXTAREA = 'textarea',
  SWITCH = 'switch',
}
